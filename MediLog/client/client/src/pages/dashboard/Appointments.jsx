import React from 'react';
import { Typography, Box } from '@mui/material';

const Appointments = () => {
  return (
    <Box>
      <Typography variant="h4" gutterBottom>
        Appointments
      </Typography>
      <Typography>Track and manage upcoming appointments.</Typography>
    </Box>
  );
};

export default Appointments;
