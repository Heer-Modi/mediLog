import React from 'react';
import { Typography, Box } from '@mui/material';

const Calendar = () => {
  return (
    <Box>
      <Typography variant="h4" gutterBottom>
        Reminders & Calendar
      </Typography>
      <Typography>Add and manage your appointment reminders.</Typography>
    </Box>
  );
};

export default Calendar;
